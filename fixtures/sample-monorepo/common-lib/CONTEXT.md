# common-lib

> Shared math utilities for the vault program.

## Public API

### Functions

- `pub fn bps_of(amount: u64, bps: u16) -> u64`
- `pub fn shares_to_amount(shares: u64, total_shares: u64, total_amount: u64) -> u64`

