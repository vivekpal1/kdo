# vault-sdk

> TypeScript SDK for interacting with the vault program.

## Public API

### Functions

- `export function createVaultClient(endpoint: string): VaultClient`

### Structs

- `export class VaultClient`

### Traits

- `export interface VaultState {`
- `export interface UserPosition {`

### Types

- `export type TransactionResult = {   signature: string;   slot: number; };`

