{
  "name": "vault-sdk",
  "version": "0.1.0",
  "description": "TypeScript SDK for interacting with the vault program.",
  "main": "src/index.ts",
  "scripts": {
    "build": "echo 'tsc (skipped — no node_modules)'",
    "test": "echo 'no tests configured'",
    "lint": "echo 'no linter configured'"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {}
}
