[package]
name = "vault-program"
version = "0.1.0"
edition = "2021"
description = "Solana vault program. Handles deposits, withdrawals, fee accrual."

[dependencies]
common-lib = { path = "../../common-lib" }
